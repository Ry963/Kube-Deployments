apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-nginx-ing
  namespace: Testing
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: test-nginx-svc
            port:
              number: 80